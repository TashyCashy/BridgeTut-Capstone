package LessonTutorial;

/**
 * Enumerates expected final interactive tutorial outcomes[cite: 26].
 */
public enum LessonOutcome {
    /**
     * Indicates the lesson expects the user to claim remaining tricks to finish[cite: 26].
     */
    CLAIM,

    /**
     * Indicates the lesson expects the user to concede remaining tricks to finish[cite: 26].
     */
    CONCEDE
}